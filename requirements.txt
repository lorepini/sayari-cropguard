# Dashboard runtime dependencies — what Render installs to serve the web app.
# Pipeline-only deps (rasterio, sentinelsat, pystac-client) are in
# requirements-pipeline.txt and are NOT needed to run the dashboard.

# Core geospatial (dashboard needs geopandas for community GeoJSON)
geopandas==1.0.1
shapely==2.0.6
numpy==1.26.4
pandas==2.2.3
pyproj==3.6.1

# HTTP
requests==2.32.3

# Machine learning + statistics
scikit-learn==1.5.2
scipy==1.13.1
joblib==1.4.2

# LLM alerts
anthropic==0.40.0

# Dashboard
dash==2.18.2
dash-bootstrap-components==1.6.0
plotly==5.24.1
flask-cors==4.0.1

# Utilities
python-dotenv==1.0.1
tqdm==4.66.5
pyarrow==17.0.0
